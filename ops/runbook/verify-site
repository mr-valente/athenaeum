#!/usr/bin/env python3
"""Verify public HTTPS routes and container metadata isolation without changing data."""
import argparse
from _support import finish, installed_config
from athenaeum_ops.verification import NoRedirect, probe, verify


def main():
    argparse.ArgumentParser(description=__doc__).parse_args()
    verify(installed_config())


if __name__ == '__main__':
    finish(main)
